# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (if used later)
Pods/
Carthage/Build/

# Secrets — never commit real API keys. The Sources/Secrets.swift stub
# IS committed (empty placeholder keys); developers override values
# locally without committing changes. Use *.env / APIKeys.plist for any
# additional out-of-band secrets.
*.env
.env
APIKeys.plist

# Local tooling / scratch — never tracked
.claude/
doc/

# Design assets — local-only, not tracked
Design System/
